/runtime
/.idea
/.vscode
/vendor
*.log
.env
.env.lock
.env.tmp.*
.env.*.local
/tests/tmp
/tests/.phpunit.result.cache

# bin 中的 CLI、脚本和已构建 helper 都属于明确要求纳入版本控制的交付文件。
!/bin/
!/bin/**

# Composer vendor 按交付要求纳入版本控制；依赖升级后必须与 composer.lock 同步提交。
!/vendor/
!/vendor/**
/vendor/phpunit/phpunit/.phpunit.result.cache
